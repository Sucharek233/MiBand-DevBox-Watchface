-- Global, so I don't have to import it every single time :)
MailboxStates = {
    IDLE = "idle",
    PENDING = "pending",
    RUNNING = "running",
    DONE = "done",
    ERROR = "error",
    TIMEOUT = "timeout",
    STREAM = "stream"
}