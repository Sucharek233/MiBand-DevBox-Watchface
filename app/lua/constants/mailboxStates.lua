-- Global, so I don't have to import it every single time :)
MailboxStates = {
    -- IDLE = "idle",
    -- PENDING = "pending",
    -- RUNNING = "running",
    -- DONE = "done",
    -- ERROR = "error",
    -- TIMEOUT = "timeout",
    -- STREAM = "stream"

    DONE = 0,
    IDLE = 1,
    PENDING = 2,
    RUNNING = 3,
    ERROR = 4,
    TIMEOUT = 5,
    STREAM = 6
}